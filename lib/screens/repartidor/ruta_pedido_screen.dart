// lib/screens/ruta_pedido_screen.dart
import 'package:flutter/material.dart';

class RutaPedidoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: Text('Ruta Pedido (Placeholder)')),
    body: Center(child: Text('Ruta Pedido View')),
  );
}
