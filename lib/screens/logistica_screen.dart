// lib/screens/logistica_screen.dart
import 'package:flutter/material.dart';

class LogisticaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: Text('Logística (Placeholder)')),
    body: Center(child: Text('Logística View')),
  );
}
